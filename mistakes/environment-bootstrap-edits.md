# 脚手架编辑与确认状态防呆

日期：2026-09-24；分类：environment / rules。

- 补丁同次删除并新增同一路径被工具拒绝；改为单次更新或直接写入已确认内容。
- 通过 JavaScript 模板字符串传递 Markdown 时，未转义的反引号导致语法错误；后续使用不冲突的文本传参方式。两次失败均发生在写入前，未覆盖文件。
- 日程编辑检查发现住宿确认状态有沿用旧值的风险；表单改为同时要求重新核实行程和住宿确认状态。
- 地图部分图块失败不应描述为整张底图不可用；改为局部图块提示，成功加载后清除提示。
- 旅行正式页面的浏览器存储与测试隔离：正式端口 8788，测试端口 8789。

- 内置浏览器的下载事件等待超时，不能据点击认定文件下载成功；增加可见 JSON 与复制导出，并验证剪贴板内容可解析。
