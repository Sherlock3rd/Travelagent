# 错题与复盘

每次任务先检索本目录，无相关记录时检索 `D:\charlie\Workbencch\mistakes\`。现有记录：[编辑与确认状态防呆](environment-bootstrap-edits.md)。

使用 `mistake-template.md` 记录错误，按 environment、rules、sources、deployment 分类；同类问题合并，并把预防检查加入执行流程。

环境防呆：确认 Git remote 和工作目录；确认端口实际进程；不得将仓库根目录当作站点目录；发布前检查私密材料忽略状态。
