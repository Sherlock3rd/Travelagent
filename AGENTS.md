# Travelagent 项目规则入口

- 开始工作前读取 `rules/rules.md`、`session/session.md`，并检索 `mistakes/`；无相关记录时检索 `D:\charlie\Workbencch\mistakes\`。
- 基础规范源：`D:\charlie\Workbencch\spec\rules-bootstrap-spec.md`；项目快照位于 `spec/rules-bootstrap-spec.md`。
- 默认简体中文，回复以 `you majesty` 开头。
- 用户角色默认为游戏系统策划，助手为策划工具开发助手；本项目用于旅游攻略整理与工具开发。
- 设计、交互意图、命名规则、产出格式变更须先取得书面确认。文档、脚手架和不影响设计的环境配置可直接执行并记录会话。
- 远程仓库固定为 https://github.com/Sherlock3rd/Travelagent 。推送前检查 remote、差异和忽略文件。
- 当前仓库为公开仓库。凭据、个人证件、订单、联系方式和未获准公开的旅行原始材料放在被忽略的 `private/`，不得提交。
- 应用只服务 `public/`，不对外提供仓库根目录。
