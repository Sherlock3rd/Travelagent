# 全设备服务器同步

用户要求：修改同步所有设备，数据存在服务器。沿用此前明确选择的公开、暂不验证访问方式。

完成范围：全部旅行内容统一存入 Supabase；前端自动读取、保存、轮询同步；本机缓存及草稿保护；并发合并、冲突拒绝、幂等请求与 100 个历史版本。

初始化从线上与本地可见导出核对，两端内容及 revision 23 完全一致；未使用旧素材重建。双端浏览器测试清单和留言同步，测试后移除临时留言、恢复勾选，完整原资料核对一致。

实现说明与维护见 docs/cloud-sync.md。
